<%= form_for(@product_template) do |f| %>
  <% if @product_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_template.errors.count, "error") %> prohibited this product_template from being saved:</h2>

      <ul>
      <% @product_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
